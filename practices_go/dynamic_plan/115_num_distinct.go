package dynamicplan

func numDistinct(s string, t string) int {
	/*
		115. 不同的子序列
		给你两个字符串 s 和 t ，统计并返回在 s 的 子序列 中 t 出现的个数。

		输入：s = "rabbbit", t = "rabbit"
		输出：3

		输入：s = "babgbag", t = "bag"
		输出：5
	*/
	return 0

}
